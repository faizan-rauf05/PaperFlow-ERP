/**
 * Legacy M2 exit script — rolls removed.
 * Prefer manual smoke tests against Admin Materials / Production / Inventory.
 */
console.log("m2-exit-test skipped: PaperRoll module removed. Use Admin UI smoke tests instead.");
process.exit(0);
